대시보드
 - 클러스터별 워크로드 갯수, 워크로드별 노드 사용율(파드 갯수, 파드의 cpu, gpu, disk, memory, network, diskIO, networkIO)
 
 Workload Request
    최신 리퀘스트 리스트 순으로 정열 (완료 된것 검색은 차후) - 팝업으로 상세요청 보기
	- 이름, 요청시간, 파드 갯수, 순서, 배포통지 시간, 최초 배포시간, 전체 완료시간, 배포 클러스터, 파드보기(아래 테이블 생성 토글), 전체요청보기(팝업)
	   - 파드 명, 순서, 배포노드, 리소스 요청 정보, 예상시간, 생성시간, 스케줄링시간, 예상스케줄링시간, 완료시간, 예상완료시간
	   
Setting
	- 운영리소스 최소 용량
	  입력폼, 노드가 운영에 필요한 최소 용량 설정, 계산식에 적용(cpu, 메모리, 디스크, gpu는 없어도 운영에 영향 없음) 
	- 파드 스케일링(up-down) 정책
	  입력폼, 파드가 limits의 x%(예:90%) 이상 얼마동안 유지할때( gpu도 각 개별 사용량이 0~1사이이므로 계산이 가능함 , gpu는 개별 gpu 사용량으로 설정) up
	  아니민 reqeusts 에서 머무를때 limit 조정(down)
	  request 파드가 x%(예:60%) 이하일때 reqeusts, limits 조정(down)
	  
	- 노드 스케일링(in-out)정책 - 알람
	  노드의 cpu 몇%이상 지속시간, 메모리 % 이상 지속시간, 디스크 부족 % 이상, 워크로드 배치 지연 건수 몇건 이상
	  
	  -------------------시나리오 관련 설정을 위함, 실제 정책에서 할 일은 아닌듯
	- 노드 특성 정책
	  노드레벨 설정, 클러스터 특성 설정
	  클러스터, 노드, cpu, 메모리, 디스크, gpu 용량, cpu 클럭
	  클러스터, 퍼블릭, private
	  
	 - 워크로드 특성정책
	    - 일단 리스트 만 생성
	 